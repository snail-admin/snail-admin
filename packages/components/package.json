{
  "name": "@snail-admin/components",
  "version": "1.0.0",
  "description": "all components are settled here",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "peerDependencies": {
    "vue": "^3.2.0"
  }
}
